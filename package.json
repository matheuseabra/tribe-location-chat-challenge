{
  "name": "tribe-location-chat-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "private": true,
  "scripts": {
    "build:client": "cd packages/client && npm run build",
    "build:server": "cd packages/server && npm run build",
    "dev:server": "cd packages/server && yarn dev:server",
    "dev:client": "cd packages/client && yarn start"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.0.3"
  }
}
